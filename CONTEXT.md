# Stock Matrix

A 股热力图应用的领域词汇表。单一上下文。

## Language

### 界面外观

**Skin（皮肤）**:
用户可选的两套界面视觉皮肤：`classic`（不透明卡片+边框）与 `ios26`（Liquid Glass 毛玻璃）。设置面板中显示为"界面风格"。皮肤只改变样式，不改变结构与行为。
_Avoid_: 主题、design style（代码标识符 `designStyle` 除外）

**Display mode（显示模式）**:
亮色 / 暗色配色模式。与皮肤（Skin）正交：任意皮肤 × 任意显示模式都有效。
_Avoid_: 主题（易与皮肤混淆）

**Heatmap（热力图）**:
按流通市值分配面积、按涨跌幅着色的 Canvas 画布，是应用的主体视图。

### 色块与聚合

**色块（Tile）**:
热力图中代表一只股票、一个子板块或一个板块的矩形。面积按流通市值权重分配，颜色按涨跌幅。
_Avoid_: 格子、方块

**其他块（Overflow tile）**:
同一展示区域内"放不下名字"的色块合并成的单个替身色块，面积等于成员市值之和。成员没有被丢弃——点击其他块可以看到成员列表并进入个股。
_Avoid_: 聚合块、隐藏（成员仍可到达，不是隐藏）

**放不下名字（Unreadable）**:
在当前画布尺寸、缩放倍数与标签显示规则下，色块无法完整显示股票名的状态。放不下名字的色块并入其他块。
_Avoid_: 小色块（口语可用，是否聚合以本条判定为准）
